```markdown
# 2026新年倒计时网页 - 代码文档

## 📋 项目概述

这是一个交互式新年倒计时网页，具有以下特效：
- 字符雨背景动画
- 蓝白色粒子倒计时（10-1秒）
- 新年祝福语展示
- 烟花爆炸效果
- 表情粒子飘落
- 背景音乐播放控制

**作者**: hellohmj  
**创建日期**: 2025-12-18  
**用途**: 送给朋友的跨年祝福网页

---

## 🎨 页面结构

### HTML 元素

```html
<!-- 音乐控制按钮 -->
<div id="musicControl">

<!-- 背景音乐 -->
<audio id="bgMusic">

<!-- 字符雨背景画布 -->
<canvas id="bgCanvas">

<!-- 粒子倒计时画布 -->
<canvas id="particleCanvas">

<!-- 祝福语容器 -->
<div id="blessing">

<!-- 烟花效果画布 -->
<canvas id="fireworkCanvas">

<!-- 表情粒子画布 -->
<canvas id="emojiCanvas">
```

---

## 🎵 音乐控制模块

### 功能说明
- 右上角显示音乐控制按钮
- 点击切换播放/暂停
- 页面加载时自动尝试播放
- 支持用户首次点击页面时播放

### 关键变量
```javascript
let bgMusic          // 音频元素
let musicIcon        // 音乐图标元素
let isMusicPlaying   // 音乐播放状态标志
```

### 核心函数

#### `toggleMusic()`
**功能**: 切换音乐播放状态

**逻辑**:
```javascript
if (isMusicPlaying) {
    // 暂停音乐，图标变为🔇
} else {
    // 播放音乐，图标变为🔊
}
```

#### 自动播放事件监听
```javascript
// 页面加载后尝试自动播放
window.addEventListener('load', ...)

// 用户首次点击页面时播放
document.addEventListener('click', playOnFirstClick)
```

**注意**: 现代浏览器限制自动播放，可能需要用户交互后才能播放音乐。

---

## 🌧️ 字符雨背景模块

### 功能说明
创建矩阵风格的字符雨背景，显示"2026新年快乐HAPPYNEWYEAR"字符。

### 关键变量
```javascript
var bgCanvas     // 背景画布元素
var bgCtx        // 2D绘图上下文
var texts        // 字符数组: ['2', '0', '2', '6', '新', '年'...]
var fontSize     // 字符大小: 16px
var columns      // 列数 = 画布宽度 / 字符大小
var drops[]      // 每列字符的Y坐标数组
```

### 核心函数

#### `drawBg()`
**功能**: 绘制字符雨的每一帧

**执行步骤**:
1. 绘制半透明黑色背景（实现拖尾效果）
   ```javascript
   ctx.fillStyle = 'rgba(0, 0, 0, 0.05)'
   ```

2. 随机选择字符并绘制
   ```javascript
   var text = texts[Math.floor(Math.random() * texts.length)]
   ```

3. 更新字符Y坐标（向下移动）
   ```javascript
   drops[i]++
   ```

4. 字符到达底部或随机重置
   ```javascript
   if (drops[i] * fontSize > canvas.height || Math.random() > 0.95) {
       drops[i] = 0  // 重置到顶部
   }
   ```

**刷新频率**: 每33ms执行一次 (约30fps)

---

## ⏱️ 粒子倒计时模块

### 功能说明
使用蓝白色粒子组成的数字进行倒计时（10→9→...→1），每个数字由粒子聚合形成，然后爆炸飞散。

### 关键变量
```javascript
let particleCanvas        // 粒子画布
let particleCtx          // 2D绘图上下文
let countdownParticles[] // 倒计时粒子数组
let currentNumber        // 当前倒计时数字 (初始10)
let isCountingDown       // 倒计时进行中标志
let exploding            // 爆炸状态标志
let explodeTimer         // 爆炸计时器
```

### 类: `CountdownParticle`

#### 构造函数
```javascript
constructor(x, y)
```
**参数**:
- `x, y`: 粒子的目标位置（基准位置）

**属性**:
- `baseX, baseY`: 目标位置坐标
- `x, y`: 当前位置（初始随机偏移）
- `vx, vy`: 速度向量
- `life`: 生命值 (0-1)
- `size`: 粒子大小 (3-7px)
- `isBlue`: 是否为蓝色粒子
- `blueShade`: 蓝色亮度值 (150-250)

#### 核心方法

##### `update()`
**功能**: 更新粒子位置（吸引到目标位置）

**物理模拟**:
```javascript
// 计算指向目标的力
let dx = this.baseX - this.x
let dy = this.baseY - this.y

// 加速度 = 距离 × 0.02
this.vx += dx * 0.02
this.vy += dy * 0.02

// 速度衰减（摩擦力）
this.vx *= 0.9
this.vy *= 0.9

// 更新位置
this.x += this.vx
this.y += this.vy
```

##### `draw()`
**功能**: 绘制粒子

**渲染逻辑**:
```javascript
if (this.isBlue) {
    // 蓝色粒子: rgba(100, 150, 150-250, 不透明度)
    fillStyle = `rgba(100, 150, ${this.blueShade}, ${this.life})`
    shadowColor = '#66b3ff'  // 浅蓝色光晕
} else {
    // 白色粒子: rgba(255, 255, 255, 不透明度)
    fillStyle = `rgba(255, 255, 255, ${this.life})`
    shadowColor = '#ffffff'  // 白色光晕
}
```

##### `explode()`
**功能**: 粒子爆炸效果

**逻辑**:
```javascript
// 随机设置新的目标位置（远离当前位置）
this.baseX = this.x + (Math.random() - 0.5) * 400
this.baseY = this.y + (Math.random() - 0.5) * 400

// 随机速度
this.vx = (Math.random() - 0.5) * 10
this.vy = (Math.random() - 0.5) * 10
```

##### `fadeOut()`
**功能**: 淡出效果
```javascript
this.life -= 0.02  // 每帧减少生命值
```

### 核心函数

#### `createNumberParticles(num)`
**功能**: 根据数字创建粒子

**参数**: `num` - 要显示的数字

**执行步骤**:

1. **创建临时画布**
   ```javascript
   let tempCanvas = document.createElement('canvas')
   let fontSize = Math.min(particleCanvas.width * 0.5, 500)
   ```
   - 字体大小 = 屏幕宽度的50%（最大500px）

2. **在临时画布上绘制数字**
   ```javascript
   tempCtx.font = `bold ${fontSize}px Arial`
   tempCtx.fillText(text, tempCanvas.width / 2, tempCanvas.height / 2)
   ```

3. **获取像素数据**
   ```javascript
   let imageData = tempCtx.getImageData(0, 0, tempCanvas.width, tempCanvas.height)
   let pixels = imageData.data
   ```

4. **采样像素，创建粒子**
   ```javascript
   let gap = 5  // 每隔5像素采样一次
   for (let y = 0; y < height; y += gap) {
       for (let x = 0; x < width; x += gap) {
           if (pixels[index + 3] > 128) {  // alpha > 128 的像素
               // 创建粒子
               countdownParticles.push(new CountdownParticle(px, py))
           }
       }
   }
   ```

#### `animateCountdown()`
**功能**: 倒计时动画主循环

**执行流程**:
```javascript
1. 清空画布
2. 更新并绘制所有粒子
3. 如果处于爆炸状态:
   - 让粒子淡出
   - 计时器+1
   - 如果计时器>30: 切换到下一个数字
4. 请求下一帧
```

#### `nextCountdownNumber()`
**功能**: 切换到下一个倒计时数字

**逻辑**:
```javascript
currentNumber--

if (currentNumber >= 1) {
    // 创建新数字的粒子
    createNumberParticles(currentNumber)
    
    // 800ms后开始爆炸
    setTimeout(() => {
        exploding = true
        countdownParticles.forEach(p => p.explode())
    }, 800)
} else {
    // 倒计时结束，显示祝福语
    isCountingDown = false
    particleCanvas.style.display = 'none'
    showBlessing()
}
```

### 倒计时时序图
```
10 → [聚合800ms] → [爆炸30帧] → 9 → [聚合800ms] → [爆炸30帧] → ... → 1 → 祝福语
```

---

## 🎊 祝福语模块

### 功能说明
倒计时结束后显示新年祝福语，带有渐入动画效果。

### HTML结构
```html
<div id="blessing">
    <h1>🎉 2026 新年快乐 🎉</h1>
    <div class="divider"></div>
    <p class="message">✨ 愿新的一年 所有的美好如约而至 ✨</p>
    <p class="message">🌟 愿你平安喜乐 万事顺意 🌟</p>
    <p class="message">💖 愿我们的友谊地久天长 💖</p>
    <p class="message">🎊 新年新气象 一起向未来 🎊</p>
</div>
```

### CSS动画

#### 标题发光动画
```css
@keyframes glow {
    0%, 100% { 
        filter: drop-shadow(0 0 20px #ffd700)
    }
    50% { 
        filter: drop-shadow(0 0 30px #ffd700)
    }
}
```

#### 祝福语渐入动画
```css
@keyframes fadeInUp {
    from {
        opacity: 0
        transform: translateY(30px)  /* 从下方30px处开始 */
    }
    to {
        opacity: 1
        transform: translateY(0)     /* 移动到原位置 */
    }
}
```

### 动画时序
```
标题: 立即显示 + 呼吸发光
分割线: 1.5s后淡入
祝福语1: 0.3s后淡入上浮
祝福语2: 0.6s后淡入上浮
祝福语3: 0.9s后淡入上浮
祝福语4: 1.2s后淡入上浮
```

### 核心函数

#### `showBlessing()`
**功能**: 显示祝福语并启动后续效果

```javascript
function showBlessing() {
    document.getElementById('blessing').style.display = 'block'
    startFireworks()        // 启动烟花
    startEmojiParticles()   // 启动表情粒子
}
```

---

## 🎆 烟花效果模块

### 功能说明
在祝福语显示后，全屏随机位置发射烟花并爆炸成彩色粒子。

### 关键变量
```javascript
let canvas           // 烟花画布
let ctx              // 2D绘图上下文
let fireworks[]      // 烟花数组
let particles[]      // 爆炸粒子数组
```

### 类: `Firework`

#### 构造函数
```javascript
constructor()
```

**属性**:
- `x`: 发射水平位置（随机）
- `y`: 当前高度（从底部开始）
- `targetY`: 目标爆炸高度（屏幕上半部分随机）
- `speed`: 上升速度 (3-5)
- `hue`: 颜色色相 (0-360°)

#### 核心方法

##### `update()`
**功能**: 更新烟花位置并检测是否到达目标

**返回值**: `true`继续飞行, `false`已爆炸

```javascript
this.y -= this.speed  // 向上移动

if (this.y <= this.targetY) {
    this.explode()  // 到达目标高度，爆炸
    return false
}
return true
```

##### `draw()`
**功能**: 绘制飞行中的烟花

```javascript
ctx.arc(this.x, this.y, 3, 0, Math.PI * 2)
ctx.fillStyle = `hsl(${this.hue}, 100%, 50%)`
```

##### `explode()`
**功能**: 烟花爆炸，生成50个粒子

```javascript
for (let i = 0; i < 50; i++) {
    particles.push(new Particle(this.x, this.y, this.hue))
}
```

### 类: `Particle`

#### 构造函数
```javascript
constructor(x, y, hue)
```

**参数**:
- `x, y`: 爆炸中心坐标
- `hue`: 继承烟花的颜色

**属性**:
- `angle`: 发射角度（随机 0-360°）
- `speed`: 发射速度 (2-7)
- `vx, vy`: 速度分量（根据角度计算）
- `life`: 生命值 (100)
- `decay`: 衰减速度 (0.5-2)

#### 核心方法

##### `update()`
**功能**: 更新粒子状态（物理模拟）

**物理效果**:
```javascript
// 空气阻力
this.vx *= 0.98
this.vy *= 0.98

// 重力
this.vy += 0.1

// 更新位置
this.x += this.vx
this.y += this.vy

// 生命值衰减
this.life -= this.decay

return this.life > 0  // 是否存活
```

##### `draw()`
**功能**: 绘制粒子（带透明度）

```javascript
ctx.fillStyle = `hsla(${this.hue}, 100%, 50%, ${this.life / 100})`
```

### 核心函数

#### `startFireworks()`
**功能**: 启动烟花效果

```javascript
canvas.style.display = 'block'

// 每400ms发射一个烟花
setInterval(() => {
    fireworks.push(new Firework())
}, 400)

animate()  // 启动动画循环
```

#### `animate()`
**功能**: 烟花动画主循环

**执行流程**:
```javascript
1. 绘制半透明黑色背景（拖尾效果）
   ctx.fillStyle = 'rgba(0, 0, 0, 0.1)'

2. 更新并绘制所有烟花
   fireworks = fireworks.filter(fw => {
       fw.draw()
       return fw.update()  // false的会被移除
   })

3. 更新并绘制所有粒子
   particles = particles.filter(p => {
       p.draw()
       return p.update()  // false的会被移除
   })

4. 请求下一帧
   requestAnimationFrame(animate)
```

---

## 😊 表情粒子模块

### 功能说明
在屏幕上方持续飘落各种表情符号，营造欢乐氛围。

### 关键变量
```javascript
let emojiCanvas       // 表情画布
let emojiCtx          // 2D绘图上下文
let emojiParticles[]  // 表情粒子数组
const emojis[]        // 表情库: ['😊', '🎉', '🎊', '✨', '🌟', '💫', '⭐', '🎆']
```

### 类: `EmojiParticle`

#### 构造函数
```javascript
constructor()
```

**属性**:
- `x`: 水平位置（随机）
- `y`: 垂直位置（从屏幕上方-50开始）
- `targetY`: 目标高度（50-200之间）
- `emoji`: 随机选择的表情
- `speed`: 下降速度 (1-3)
- `sway`: 左右摆动速度 (-1到1)
- `life`: 生命值 (300帧)

#### 核心方法

##### `update()`
**功能**: 更新表情位置（飘落+摆动）

```javascript
this.y += this.speed      // 向下移动
this.x += this.sway       // 左右摆动
this.life--               // 生命值-1

// 到达目标高度后减速
if (this.y > this.targetY) {
    this.speed *= 0.95
}

// 生命值>0 且未离开屏幕
return this.life > 0 && this.y < canvas.height
```

##### `draw()`
**功能**: 绘制表情

```javascript
emojiCtx.font = '30px Arial'
emojiCtx.fillText(this.emoji, this.x, this.y)
```

### 核心函数

#### `startEmojiParticles()`
**功能**: 启动表情粒子效果

```javascript
emojiCanvas.style.display = 'block'

// 每300ms生成一个新表情
setInterval(() => {
    emojiParticles.push(new EmojiParticle())
}, 300)

animateEmojis()
```

#### `animateEmojis()`
**功能**: 表情动画主循环

```javascript
1. 清空画布
   emojiCtx.clearRect(0, 0, width, height)

2. 更新并绘制所有表情
   emojiParticles = emojiParticles.filter(p => {
       p.draw()
       return p.update()  // false的会被移除
   })

3. 请求下一帧
   requestAnimationFrame(animateEmojis)
```

---

## 📱 响应式设计

### 断点说明

```css
/* 小屏手机 (≤375px) */
@media screen and (max-width: 375px) {
    - 祝福语标题: 35px
    - 祝福语正文: 14px
    - 音乐按钮: 50x50px
}
```

### 自适应字体
使用 `clamp()` 函数实现流畅的字体缩放：

```css
/* 语法: clamp(最小值, 理想值, 最大值) */
font-size: clamp(40px, 8vw, 90px)
```

**示例**:
- 小屏幕: 40px
- 中等屏幕: 8% 视口宽度
- 大屏幕: 90px

### Canvas自适应
```javascript
window.addEventListener('resize', () => {
    // 重新设置所有canvas尺寸为窗口尺寸
    bgCanvas.width = window.innerWidth
    bgCanvas.height = window.innerHeight
    // ... 其他canvas
})
```

---

## 🎨 视觉效果详解

### 1. 发光效果 (Glow)
**实现方式**: `shadowBlur` + `shadowColor`

```javascript
ctx.shadowBlur = 20           // 模糊半径
ctx.shadowColor = '#66b3ff'   // 发光颜色
ctx.fill()
ctx.shadowBlur = 0            // 重置（防止影响后续绘制）
```

### 2. 拖尾效果 (Trail)
**实现方式**: 半透明背景叠加

```javascript
// 不完全清空画布，而是绘制半透明黑色
ctx.fillStyle = 'rgba(0, 0, 0, 0.1)'
ctx.fillRect(0, 0, canvas.width, canvas.height)
```

**原理**: 每帧覆盖10%透明度的黑色，旧内容逐渐变暗消失。

### 3. 渐变文字 (Gradient Text)
```css
background: linear-gradient(45deg, #ffd700, #ffed4e, #ffd700);
-webkit-background-clip: text;
-webkit-text-fill-color: transparent;
```

**原理**: 将渐变背景裁剪为文字形状。

### 4. 物理模拟
**粒子吸引力**:
```javascript
// 弹簧力: 力 ∝ 距离
force = (target - current) * stiffness
velocity += force
velocity *= damping  // 阻尼
position += velocity
```

**重力效果**:
```javascript
vy += gravity  // 每帧增加向下的速度
y += vy        // 更新位置
```

---

## 🔧 性能优化

### 1. 使用 `requestAnimationFrame`
```javascript
// ✅ 好的做法
function animate() {
    // 绘制逻辑
    requestAnimationFrame(animate)
}

// ❌ 避免使用
setInterval(animate, 16)  // 不会自动暂停
```

**优势**:
- 自动适配屏幕刷新率
- 标签页不可见时自动暂停
- 更流畅的动画

### 2. 粒子过滤
```javascript
// 只保留存活的粒子
particles = particles.filter(p => p.update())
```

**效果**: 自动移除死亡粒子，避免内存泄漏。

### 3. Canvas分层
```
bgCanvas (背景层) → particleCanvas (倒计时层) → fireworkCanvas (烟花层) → emojiCanvas (表情层)
```

**优势**:
- 不同特效互不干扰
- 可以独立显示/隐藏
- 降低重绘开销

---

## 📂 文件结构

```
新年网页/
├── index.html          # 主文件（包含所有代码）
└── README.md          # 本文档
```

---

## 🚀 使用方法

### 基本使用
1. 将代码保存为 `index.html`
2. 用浏览器打开
3. 点击右上角音乐按钮播放音乐
4. 观看倒计时和特效

### 自定义修改

#### 修改倒计时秒数
```javascript
let currentNumber = 10  // 改为你想要的秒数
```

#### 修改祝福语
```html
<p class="message">✨ 你的祝福语 ✨</p>
```

#### 更换背景音乐
```html
<audio id="bgMusic" loop>
    <source src="你的音乐文件.mp3" type="audio/mpeg">
</audio>
```

#### 修改字符雨内容
```javascript
var texts = '你想显示的文字'.split('')
```

#### 调整烟花密度
```javascript
setInterval(() => {
    fireworks.push(new Firework())
}, 400)  // 改小数字=更多烟花，改大数字=更少烟花
```

---

## 🐛 已知问题与解决方案

### 问题1: 音乐无法自动播放
**原因**: 浏览器限制自动播放策略

**解决方案**:
- Chrome: 点击页面任意位置后音乐会自动播放
- Safari: 点击右上角音乐按钮手动播放
- Firefox: 在浏览器设置中允许自动播放

### 问题2: 手机上粒子效果卡顿
**原因**: 粒子数量太多，移动设备性能不足

**解决方案**:
```javascript
// 减小粒子密度
let gap = 8  // 从5改为8

// 减少烟花粒子数
for (let i = 0; i < 30; i++) {  // 从50改为30
    particles.push(new Particle(this.x, this.y, this.hue))
}
```

### 问题3: 字体在某些手机上显示异常
**解决方案**:
```css
font-family: -apple-system, BlinkMacSystemFont, "STKaiti", "KaiTi", sans-serif;
```

---

## 🎯 技术要点总结

### JavaScript
- Canvas 2D API 绘图
- 面向对象编程（ES6 Class）
- requestAnimationFrame 动画循环
- 数组高阶函数（filter, forEach, map）
- 事件监听器（addEventListener）
- 音频API（HTMLAudioElement）

### CSS
- Flexbox 布局
- CSS动画（@keyframes）
- 媒体查询（@media）
- CSS渐变（linear-gradient）
- 背景裁剪（background-clip）
- 滤镜效果（filter: drop-shadow）
- clamp() 响应式函数

### Canvas技巧
- 多层Canvas叠加
- 粒子系统设计
- 物理模拟（弹簧力、重力）
- 阴影与发光效果
- 像素数据处理（getImageData）

---

## 📊 性能指标

### 预期性能
- **桌面端**: 60 FPS (流畅)
- **平板**: 45-60 FPS
- **手机**: 30-45 FPS

### 资源占用
- **内存**: 约50-100MB
- **CPU**: 中等（主要用于Canvas绘制）
- **GPU**: 低（2D Canvas硬件加速）

---

## 🌟 特效时间线

```
0s     ━━━━━━━━━━━━━━━━━━━━━━━━━━━━
       │ 页面加载
       │ 字符雨背景开始
       │ 倒计时10出现
       │
1s     │ 倒计时10爆炸
       │
2s     │ 倒计时9出现
       │
3s     │ 倒计时9爆炸
       │
       ⋮ (重复...)
       │
11s    │ 倒计时1爆炸
       │
12s    │ 祝福语淡入
       │ 烟花开始
       │ 表情粒子开始
       │
∞      │ 持续烟花+表情粒子
       ━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 💡 扩展思路

### 可以添加的功能
1. **倒计时音效**: 每个数字出现时播放"滴答"声
2. **鞭炮效果**: 在倒计时结束时播放鞭炮动画
3. **雪花效果**: 冬季主题，添加飘落的雪花
4. **用户输入**: 让用户自定义祝福语
5. **分享功能**: 添加分享到社交媒体的按钮
6. **全屏按钮**: 支持全屏模式
7. **主题切换**: 蓝白/红金/彩虹等多种配色方案

### 技术升级方向
1. 使用 WebGL 提升性能
2. 添加 3D 粒子效果（Three.js）
3. 使用 Web Audio API 制作音效
4. 添加触摸交互（手指触碰产生烟花）
5. 保存为 PWA（离线可用）

---

## 📝 版本历史

### v1.0 (2025-12-18)
- ✅ 初始版本
- ✅ 字符雨背景
- ✅ 粒子倒计时（蓝白色）
- ✅ 新年祝福语
- ✅ 烟花效果
- ✅ 表情粒子
- ✅ 背景音乐控制
- ✅ 响应式设计

---

## 📧 联系方式

**作者**: hellohmj  
**创建日期**: 2025-12-18  
**用途**: 跨年祝福网页

---

## 📄 许可证

本项目代码可自由使用、修改和分享。  
请保留作者信息。

---

## 🙏 致谢

- 字符雨灵感来源于《黑客帝国》
- 粒子系统参考了多个开源项目
- 音乐资源来自免费音乐库

---

**祝你2026新年快乐！🎉🎊✨**
```
